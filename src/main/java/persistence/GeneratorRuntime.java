package persistence;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Cette classe est generee une seule fois. Elle sera regeneree si elle est effacee.
 *
 * @author c-generator (c) Cardiweb
 */
public class GeneratorRuntime extends GeneratorRuntimeDefault {

	private static Logger logger = LoggerFactory.getLogger(GeneratorRuntime.class);

}
